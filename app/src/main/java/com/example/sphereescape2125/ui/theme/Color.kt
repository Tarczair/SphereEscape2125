package com.example.sphereescape2125.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Zbiór definicji prymitywów kolorystycznych (paleta surowa).
 *
 * Plik ten zawiera stałe wartości [Color] zdefiniowane szesnastkowo.
 * Kolory te nie są używane bezpośrednio w widokach, lecz są mapowane
 * na role semantyczne (np. Primary, Background) w pliku `Theme.kt`.
 */

// --- Warianty dla motywu Ciemnego (Pastelowe/Jasne) ---

/** Jasny odcień fioletu (Tone 80). */
val Purple80 = Color(0xFFD0BCFF)

/** Jasny odcień szaro-fioletowy (Tone 80). */
val PurpleGrey80 = Color(0xFFCCC2DC)

/** Jasny odcień różu (Tone 80). */
val Pink80 = Color(0xFFEFB8C8)

// --- Warianty dla motywu Jasnego (Nasycone/Ciemne) ---

/** Nasycony fiolet (Tone 40). */
val Purple40 = Color(0xFF6650a4)

/** Nasycony szaro-fiolet (Tone 40). */
val PurpleGrey40 = Color(0xFF625b71)

/** Nasycony róż (Tone 40). */
val Pink40 = Color(0xFF7D5260)

// --- Kolory Niestandardowe ---

/** Głęboki, palony pomarańcz. Używany jako specjalny kolor akcentujący. */
val Orange = Color(0xFFBF360C)